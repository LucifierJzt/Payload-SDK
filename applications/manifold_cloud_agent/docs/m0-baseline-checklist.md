# M0 基线采集清单

本清单对应实施计划的 M0“能力验证与安全契约冻结”。本步骤仅采集版本与环境证据：**不安装应用、不调用 PSDK、不执行任何飞行、云台或相机控制操作**。

请在计划用于联调的同一套 Manifold 3、M4TD、遥控器/机场与相机组合上完成采集。不要记录设备证书、私钥、token、签名 URL、序列号或其他凭据；序列号如需关联，请使用项目内约定的脱敏设备标识。

## M0-BASELINE-001：固定联调基线

### 1. 妙算 3 Linux 环境

在妙算终端执行以下只读命令，并保存输出：

```bash
uname -a
cat /etc/os-release
getconf LONG_BIT
```

记录：

| 项目 | 结果 |
|---|---|
| 采集时间（含时区） | 待填写 |
| 妙算型号/硬件版本 | 待填写 |
| Linux 内核 | 待填写 |
| 发行版/系统版本 | 待填写 |
| CPU 架构（预期 aarch64 / 64 位） | 待填写 |

### 2. DJI 设备与软件组合

从 DJI Pilot 2、DJI Assistant 或已批准的设备信息界面读取；不要依赖记忆填写。

| 项目 | 结果 |
|---|---|
| 飞行器型号（预期 M4TD） | 待填写 |
| 飞行器固件版本 | 待填写 |
| 遥控器/机场型号及固件版本 | 待填写 |
| DJI Pilot 2 版本 | 待填写 |
| 可见光/红外相机与挂载固件版本 | 待填写 |
| 联调时是否接入机场（是/否） | 待填写 |

### 3. PSDK 工程基线

本仓库当前计划基线是 Payload SDK `3.16.0 (build 2338)`。请确认妙算端实际安装/运行的 PSDK Demo（如有）与下列工程版本不存在未记录差异。

| 项目 | 结果 |
|---|---|
| 本仓库 commit（`git rev-parse --short HEAD`） | 待填写 |
| `psdk_lib/include/dji_version.h` 中的 SDK 版本 | 待填写 |
| 妙算端已安装 PSDK 应用名称/版本（如有） | 待填写 |
| 用户 App ID 已可用于联调（是/否；不填写 ID） | 待填写 |

### 4. M0 结论

- [ ] 上述组合已固定，后续 HIL 结果均可关联到此基线。
- [ ] 已确认本次采集没有执行飞行、云台、相机或存储写入操作。
- [ ] 若任一版本变更，将新建一条基线记录，并使旧 HIL 证据失效，直到重新验证。

## 交付给 Codex 的信息

请发送第 1～3 节的表格结果或等价终端输出/截图，并说明是否有任何版本无法读取。收到后，我会：

1. 将验证结果追加至 `progress.md`；
2. 把该硬件组合写入应用的受控 M4TD Profile 输入；
3. 准备下一小步：只读 PSDK 生命周期与 AircraftInfo 探针。
